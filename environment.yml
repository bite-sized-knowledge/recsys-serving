name: recsys-env
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn
      - python-dotenv
      - pymysql
      - sqlalchemy 
      - numpy
      - pandas
      - pydantic
      - pydantic-settings
